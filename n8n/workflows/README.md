# n8n Workflow Source

Canonical production workflow ID:

`L1u2xZkgFpi7KEuv`

This JSON was extracted read-only from the Mac n8n database and sanitized for
source control.

Sanitizer replacement count during Stage 3:

`1`

The live n8n database and encryption key remain outside Git.
