{
  "schema_version": 1,
  "max_queries_per_source_cycle": 2,
  "max_location_plans_per_source_cycle": 2,
  "minimum_samples_before_adaptive_weighting": 3,
  "exploration_share": 0.35,
  "runtime_penalty_reference_seconds_per_request": 120,
  "weights": {
    "new_eligible_rate": 0.55,
    "eligible_rate": 0.30,
    "telegram_rate": 0.10,
    "error_rate_penalty": 0.35,
    "runtime_penalty": 0.05
  },
  "notes": "Unseen and under-sampled queries rotate before empirical weighting. No query is down-ranked from a single smoke run."
}
